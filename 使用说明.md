# 爱丽丝的摇篮 修改器 v1.2

针对 **Alice in Cradle（爱丽丝的摇篮）v0.30f**（Unity + Mono，64 位）的**外部内存修改器**。
同时兼容 **v0.29a**（两个版本的游戏目录都认，`_debug.txt` 的开关按版本自动适配）。

- **不想研究原理就只看第一页**：打开默认停在「一键」页，连上游戏后点几下就有效果（见第二节）
- **本工具就在游戏目录里**：`…\<游戏根目录>\AIC修改器\`，会自己认出游戏（按相对位置探测），
  所以换电脑、改盘符、换路径都不用重新配置。v0.30 官方压缩包解开后是多层嵌套目录
  （`…Win ver030f\AliceInCradle Win ver030\AliceInCradle_ver030\`），
  放在**任意一层**都能认出来
- **不修改游戏任何文件**（唯一例外是「调试开关」页，且写入前自动备份）
- **不注入 DLL、不 hook、不联网**
- **纯 Python 标准库**，无需安装任何第三方包
- **支持高分屏**：声明 DPI 感知按物理像素渲染，界面不会发虚；顶栏「界面缩放」可整体调大调小
  （详见第四节）
- 实机自检结果：附加成功、检测到 Mono 运行时、可写内存 **531 个区域 / 301 MB**、
  全进程扫描耗时 **0.42 秒**、扫描结果即时复核一致率 **99.6%**；
  「一键」页首次自动查找耗时 **2~3 秒**

---

## 一、快速开始

1. 双击 **`启动修改器.bat`**
2. 在「一键」页点 **「启动游戏并连接」**（游戏已在运行时点 **「我已经开了游戏，直接连接」**）
   - 本作加载较慢，工具会**等游戏加载完**再连接（太早连会什么都扫不到）
   - 也可以勾上「游戏启动后自动连接」，以后开游戏就不用管了
3. 看到那行灰字变成绿色的 **「已经连上游戏了」** 就成功了
4. 接下来要什么就直接点：想要免伤/秒怪/全技能 → 点第 2 步的开关；
   想让金币花不掉 → 走第 3 步（填一次数字 + 点几下按钮）

启动时会自动做两件事：
- **自动备份一次存档**（存到 `backup/` 目录，30 分钟内不重复）
- 自动套用已保存的**预设**（第一次用还没有预设）

---

## 二、一键搞定（新手照着做）

打开修改器默认就在这一页。**不需要懂任何原理**，按 1→2→3 的顺序点即可。

### 2.1 第 1 步：连上游戏

| 情况 | 怎么做 |
|---|---|
| 游戏还没开 | 点 **「启动游戏并连接」**，然后等 —— 工具会等游戏加载完再连（约 30 秒） |
| 游戏已经在跑 | 点 **「我已经开了游戏，直接连接」** |
| 什么都不想点 | 勾上 **「游戏启动后自动连接」**，以后开游戏就自动连上 |

### 2.2 第 2 步：想要什么效果，点一下就开

这一组是**游戏官方自带的调试开关**，不用扫描、不改游戏程序，点了就生效：

| 按钮 | 效果 |
|---|---|
| 免伤 | 受到的伤害变成 0（等于无敌） |
| 攻击超强 | 一下就秒掉小怪 |
| 解锁全部隐藏技能 | 学不到的技能全给 |
| 解锁相册 | 相册内容全开 |
| 解锁游戏内调试菜单 | 游戏里按 **F7** 打开官方调试菜单 |

- 按钮上的圆圈就是当前状态（`● 已开启` / `○ 未开启`），再点一下就切换
- ⚠️ **改完要重启游戏才生效**
- 点 **「全部关掉，恢复原样」** 一键还原

### 2.3 第 3 步：金币 / 物品数量，跟着点几下就找出来

数值类的东西工具没法预先知道地址（**每次开游戏地址都会变**），所以要你带着认一次。
你只需要做两件事：**填一次数字** + **在数值变化后点一下按钮**。

1. 「要找什么」选 **金币**（或物品数量）
2. 回游戏看一眼金币是多少，填到框里，点 **「开始找」**（全进程扫描约 2~3 秒，会显示百分比）
3. 去游戏里**花掉一些钱**，然后：
   - 切回修改器，点 **「我在游戏里把它【变小】了」**；或者
   - 在游戏里直接按 **F8**（不用切窗口；如果是变大就按 **F9**）
4. 重复第 3 步 1~2 次，候选会从几万个缩到 1~3 个
5. 看到 **✅ 找到了！** 就点 **「锁定它」** —— 金币再也花不掉了
6. 想以后省事再点 **「存为预设」**：下次连上游戏会自动套用

> **剩 2~3 个位置是正常的**：那是同一个数值的多份拷贝（游戏内部常常存两份），
> 它们永远同值同变，工具会**一起锁定**，不用你去分辨哪个是真的。

**没结果怎么办**

| 现象 | 原因与做法 |
|---|---|
| 提示「一个都没剩下」 | 你还没真的让数值变化，或者方向点反了 → 试另一个按钮，或点「重新开始」 |
| 一直剩很多 | 数字看错了；或者游戏显示的数和实际存的差 10 倍（137 试试填 1370 或 13） |
| 提示「没找到这个数字」 | 同上；也可以换个「要找什么」 |
| 找不到 HP / 魔力 | 这类数值游戏里没有数字显示，**直接用第 2 步的「免伤」更省事** |

### 2.4 第 4 步：万一改坏了，一键还原

| 按钮 | 作用 |
|---|---|
| 停用全部锁定 | 游戏里的数值**立刻**恢复正常变化 |
| 把游戏开关全部关掉 | 第 2 步那些开关全部关闭（重启游戏后生效） |
| 还原 `_debug.txt` 到最初的样子 | 连开关文件的内容都还原成工具第一次修改前的样子 |

**还原不会删你的存档**；附加游戏时工具还自动备份过一份（见「存档」页，随时可还原）。

### 2.5 找到之后怎么用

- 被锁定的数值在游戏里**不会再减少**（金币花不掉、物品用不完）
- 绑定过「功能槽位」的预设能用**全局热键**随时开关（见第六节）
- **重开游戏后地址会变**，直接地址可能失效 —— 回来按第 3 步再找一次、再「存为预设」即可
- 想深挖（HP / 魔力 / 移速、指针链、存档备份等）：去「高级扫描」页，走第五节的手工向导

---

## 三、六个页面

| 页面 | 用途 |
|---|---|
| **一键（新手看这里）** | 连游戏 + 官方开关一键开 + 金币/物品自动查找 + 一键还原。**默认打开的就是这一页** |
| **锁定 / 监视** | 锁定项每 100ms 自动回写（游戏内数值被"按住"）；监视项只读实时刷新 |
| **预设** | 把定位好的地址存下来，一键套用；绑定"功能槽位"后可用全局热键开关 |
| **调试开关** | 完整读写游戏自带的 `_debug.txt`（比「一键」页多出所有冷门开关） |
| **存档** | 存档备份 / 还原 / 校验 |
| **高级扫描** | Cheat Engine 式数值搜索：精确值 / 大于 / 小于 / 介于 / 未知初始值 / 变化了 / 未变化 / 增加了 / 减少了 |

### 高级扫描页的两个"省事"设计

- **智能扫描**：选「精确值」时，一次自动尝试 1/2/4/8 字节四种宽度，直接告诉你哪种命中（金币这类数值常见是 4 字节）。勾了「连浮点一起试」还会试 float/double。
- **邻域浏览**：选中一个地址后点它，会列出前后 ±512 字节的所有同类型数值 —— HP / MP / EP 这类挨着放的字段，往往一次就全找到了。

---

## 四、界面显示与缩放（高分屏）

### 4.1 为什么以前的界面发虚

Windows 在「显示缩放」大于 100% 时，会让**没有声明 DPI 感知**的程序先按 96 DPI 绘制，
再由桌面窗口管理器把整幅画面**位图放大**回物理像素。

你的显示器是 **2560×1440 / 150% 缩放**，所以老版本实际只用 1707×960 在画，
画完拉伸 1.5 倍 —— 文字笔画、表格竖线、小图标全部经过插值，看着就是「分辨率不够」。
**不是窗口小，是渲染分辨率被系统砍掉了三分之一再放大。**

### 4.2 现在怎么做的

- 启动时**声明每显示器 DPI 感知 v2**（`SetProcessDpiAwarenessContext`，失败则逐级降级），
  界面直接按 2560×1440 的真实像素绘制，不再被拉伸
- 界面整体**等比缩放**：字号走 Tk 的 `tk scaling`，而 padding、表格行高、列宽、窗口尺寸
  走统一的 `px()` 换算 —— 用的是同一个倍数，所以字号与间距严格同步，不会「字变大了但挤在一起」
- 顶栏新增 **「界面缩放」** 下拉框和 **「重置窗口」** 按钮

### 4.3 界面缩放怎么选

| 选项 | 含义 |
|---|---|
| **自动**（默认） | 跟随 Windows 显示缩放。你的机器上 = **150%**，即与老版本同样的观感，但清晰度是原生的 |
| `100%` / `125%` / … | **界面实际倍数**，与系统设置无关。想让字更大就选 `175%` / `200%` |
| `200%` | 字号约为默认的 1.33 倍；窗口会自动夹在工作区内，不会超出屏幕 |

选完会问你是否立即重启 —— 缩放改变的是字体与控件尺寸，Tk 不支持中途重排，
所以点「是」会自动拉起一个新窗口并关掉旧的（锁定项会随旧进程一起停止，请注意）。

> 窗口大小和位置会被记住，下次打开还在原处。如果拖到别的显示器或改过 Windows 缩放后
> 觉得排版不对，点 **「重置窗口」** 回到按当前缩放计算的合适尺寸。

### 4.4 界面变大后不再有东西被挤掉

界面整体放大后，「容器请求高度 > 实际可用高度」的问题会暴露出来（Tk 会把最后摆放的控件截掉）。
这一版一并处理了：

- **底部日志区**此前被主区域挤成 1 像素（等于看不见），现在固定在窗口底部、始终可见
- **顶栏按钮**此前被过长的游戏路径挤出窗口右边界，现在改成两行布局，并把长路径掐头显示
- **「调试开关」页**的二十来个开关此前有近一半看不到，现在改成可滚动区域（支持滚轮）
- 自测里加了「每一页的按钮是否都在可视区内」的断言，以后不会再悄悄回归

---

## 五、首次定位向导

> 只想改**金币 / 物品数量**的话，回到第 2.3 节用「一键」页更省事（不用懂下面这些术语）。
> 这一节是**手工精确定位**，适合 HP / 魔力 / 移速这类游戏里没有数字显示、
> 或者你想自己看清楚每一步在干什么的场景。

### 5.1 金币（最简单，建议先练手）

1. 「扫描」页 → 类型 `4 字节` → 方式 `精确值` → 输入当前金币数 → **开始扫描**
2. 回游戏，花掉一些钱（比如 137 → 100）
3. 回到修改器，输入新金币数 → 点 **再筛选**
4. 重复 2~3 轮，通常会收敛到 1~5 个地址
5. 选中它 → **锁定选中** → 锁定值填 `999999`
6. 「预设」页 → **从锁定项新建…** → 名称填"金币"，功能槽位选 `add_money —— 金币 +99999`

之后按 **F4** 就能让金币一路涨。

### 5.2 HP / 魔力（推荐用"未知初始值"起手）

1. 进游戏挨两下打，让 HP 开始掉
2. 「扫描」页 → 方式 `未知初始值` → **开始扫描**（会记录整块内存的初值，稍慢几秒）
3. 再挨打 → 方式 `减少了` → **再筛选**
4. 回血 → 方式 `增加了` → **再筛选**；反复几轮即可收敛
5. 找到后用 **邻域浏览** 一次看周围数值，通常 MP / EP 就在旁边

### 5.3 物品数量 / 技能点

用**事件**来筛最准：拾取一个道具（数量 +1）→ `增加了`；用掉一个 → `减少了`。这类数值常是 1 或 2 字节，把类型调小试。

### 5.4 移动速度 / 跳跃力度

官方没有开关，需要自己定位：方式是 `未知初始值` → 走一段路 → 看变化。改完如果手感不对，**在锁定页取消勾选即可立刻恢复**（前提是你先记下原值）。

---

## 六、全局热键

在任意窗口（含全屏游戏）下都有效。修改后需重启修改器生效。

| 热键 | 功能 | 前置条件 |
|---|---|---|
| `F1` | 无敌 / 免伤 开关 | 「预设」页有槽位 `invincible` 的预设 |
| `F2` | 无限魔力 开关 | 槽位 `infinite_mp` |
| `F3` | 一击必杀 开关 | 槽位 `one_hit_kill` |
| `F4` | 金币 +99999 | 槽位 `add_money` |
| `F5` | 速度倍率循环 ×1/×1.5/×2/×3/×5 | 槽位 `speed_cycle` |
| `F8` | **「一键」页：我刚把数值变小了**（不用切窗口） | 正在用第 2.3 节的自动查找 |
| `F9` | **「一键」页：我刚把数值变大了** | 同上 |

> F8 / F9 是给「一键」页找数值用的：在游戏里花完钱，直接按 F8 就等于点了那个蓝色大按钮，
> 不必来回切窗口。没在找数值时按它们只会提示一句，不会误操作。
>
> ⚠️ **已知取舍**：你选择了 F1~F5。游戏的 `config.cfg` 里 F1~F5 被用作**手柄映射测试键**，所以热键会抢占它们。F8 在游戏里是「快速读档」类功能，**如果你用得上，就把 `config/trainer.json` 里的 `smaller` 改成别的键**（比如 `Ctrl+Alt+1`）。如果影响操作，取消顶栏的 **「启用热键」** 即可立刻释放全部热键（游戏内 F1~F5 恢复原样）。

---

## 七、调试开关页（游戏自带，零风险）

游戏的 `AliceInCradle_Data\StreamingAssets\_debug.txt` 是官方预留的调试入口。本页可以图形化开关：

| 开关 | 作用 |
|---|---|
| `mighty` | **攻击力超强**，平A秒小怪 |
| `nodamage` | **免伤**，受到的伤害降为 0 |
| `allskill` | **解锁全部隐藏技能** |
| `albumunlock` | 解锁相册内容 |
| `weak` | 纸片人模式（一碰就倒，慎用） |
| `announce` + `timestamp` | 都设为 1 后，**游戏内按 F7 可打开调试菜单** |

**安全保证**（已逐字节验证）：

- 只替换目标行的那个数字，**缩进、注释、行序、换行符全部逐字节保留**，文件长度与行数完全不变
- 首次修改前生成 `_debug.txt.orig.bak`（原始版本，永不覆盖）
- 每次修改前把当前内容存成 `_debug.txt.bak`
- 随时可点 **「还原到最初版本」** 或 **「回滚到上次修改前」**
- 也可以点 **「查看与原始的差异」** 自己确认改了什么

> 改完需要**重启游戏**才生效。

---

## 八、存档安全

- 存档位置：`%USERPROFILE%\AppData\LocalLow\NanameHacha\AliceInCradle\`
  （`savedata_00.aicsave` / `savedata_01.aicsave` / `whole.data` / `config.cfg`）
- 附加游戏时**自动备份一次**（30 分钟内不重复），也可随时点「立即备份」
- 备份落在 `backup/<时间戳>_<标签>/`，内含 `manifest.json` 与每个文件的 **sha256**，可点「校验选中」核对
- **还原前会自动再做一次 `prerestore` 安全备份**，所以还原这个动作本身也是可逆的
- 本工具**不解析也不改写** `.aicsave` 的二进制格式（那种做法容易废档），只做备份还原

---

## 九、排错 FAQ

| 现象 | 处理 |
|---|---|
| 附加失败，提示"拒绝访问" | 关掉游戏重开一次（有时启动器权限不同）；仍不行就用管理员身份运行 `启动修改器.bat` |
| 点连接后一直显示"正在加载" | 正常，本作加载要 30 秒左右；工具会**等它加载完**再连（太早连会什么都扫不到）。超过 3 分钟仍未连上就点顶栏「附加」手动重试 |
| 精确扫描 0 命中 | ① 用「智能扫描」让它自动试 1/2/4/8 字节；② 数值可能被压缩存储，试试 `2 字节` 或 `1 字节`；③ 显示值和真实值可能不同（例如显示 137 实际存 1370），把数值 ×10 / ÷10 再试 |
| 结果有几万个 | 正常，继续用「再筛选」收敛；也可以勾掉「包含映射内存」缩小范围 |
| 结果只有一个，但改了没用 | 大概率是**显示用的副本**。回到游戏让数值再变一次，用「再筛选」看哪个还跟着变 |
| 「一键」页找金币时提示"一个都没剩下" | 你还没真让金币变化，或者方向点反了 —— 试另一个按钮，或点「重新开始」重来 |
| 锁定后游戏闪退 | 先重启游戏，用「存档」页还原一份备份；下次定位时避开可疑地址（尤其别锁 `0x0000000` 附近的低地址） |
| 关掉修改器后数值还在变 | 说明有别的锁定项还在生效，或游戏自身在刷新；停用全部锁定项即可 |
| 重启游戏后预设失效 | 直接地址会变。在锁定页把地址改成 **`模块名+偏移链`** 形式（如 `AliceInCradle.exe+0x10+0x28`）即可抗重启；先用「重新定位」把新地址覆盖进预设。或者回「一键」页重新找一遍、再存一次预设 |
| 想确认工具对这个版本还好不好使 | 运行 `python tests/live_check.py`（见下） |
| 界面字太小 / 太大，或换显示器后排版不对 | 顶栏「界面缩放」选一个倍数（会问你是否自动重启）；排版乱了点「重置窗口」。详见第四节 |

---

## 十、目录结构

```
Alice in Cradle/                 ← 游戏目录
├─ AliceInCradle.exe
├─ AliceInCradle_Data/
├─ MonoBleedingEdge/
└─ AIC修改器/                    ← 本工具（就在这里）
   ├─ 启动修改器.bat             ← 双击这个
   ├─ aic_trainer.pyw            ← 程序入口
   ├─ core/                      ← 核心逻辑（与界面解耦）
   │   ├─ winmem.py             ── Win32 内存读写、模块/区域枚举、容错读取、进程就绪判定
   │   ├─ scanner.py            ── 扫描引擎
   │   ├─ autofind.py           ── 「一键」页的自动查找（多宽度并行收敛）
   │   ├─ freezer.py            ── 锁定/监视线程、地址解析（含指针链）
   │   ├─ hotkeys.py            ── 全局热键
   │   ├─ debug_flags.py        ── _debug.txt 精确读写
   │   ├─ saves.py              ── 存档备份/还原/校验
   │   ├─ presets.py            ── 预设
   │   └─ paths.py              ── 路径探测与配置
   ├─ ui/                        ← tkinter 界面
   │   ├─ scaling.py            ── 高 DPI 声明与界面缩放（字号 + 间距同步放大）
   │   ├─ main_window.py        ── 主窗口、顶栏、自动连接
   │   ├─ tab_onekey.py         ── 「一键」页（傻瓜式入口）
   │   └─ tab_*.py              ── 其余功能页
   ├─ tests/                     ← 自测脚本（见下）
   ├─ config/trainer.json        ← 配置（热键、主题、缩放、预设）
   ├─ backup/                    ← 存档备份
   └─ logs/                      ← 崩溃日志（正常情况为空）
```

### 自测脚本

```bat
cd /d "AIC修改器"
python tests\live_check.py                 :: 启动游戏→自检→关闭（推荐偶尔跑一次）
python tests\live_check.py --attach-only   :: 只附加正在运行的游戏，不动它
python tests\selftest_winmem.py            :: 内存读写
python tests\selftest_scanner.py           :: 扫描引擎
python tests\selftest_scan_robust.py       :: 内存页读不到时的容错（可读页不会连带丢失）
python tests\selftest_crossproc.py         :: 跨进程端到端
python tests\selftest_control.py           :: 锁定/热键/预设
python tests\selftest_files.py             :: 调试开关/存档（全程用临时目录，不动真实文件）
python tests\selftest_scaling.py           :: DPI 感知 / 缩放换算 / 窗口几何夹紧
python tests\selftest_onekey.py            :: 「一键」页自动查找（可远程改值的子进程冒充游戏）
python tests\selftest_restart.py           :: 改界面缩放后的自动重启
python tests\smoketest_ui.py               :: 界面冒烟测试（含"按钮有没有被挤出可视区"）
```

> 自测脚本会把配置写到临时目录（环境变量 `AIC_CONFIG_DIR`），**不会动你真实的
> `config/trainer.json`**，也不会碰游戏文件与存档。整个 suite 跑一遍约 3 分钟
> （其中 `live_check.py` 会真的启动一次游戏，其余不启动游戏）。

---

## 十一、配置项 `config/trainer.json`

| 字段 | 说明 |
|---|---|
| `game_dir` | 游戏目录。工具按「自身相对位置 → 这里的值 → 常见目录搜索」的顺序探测；本工具就在游戏目录里，所以正常情况下这里填错也没关系 |
| `hotkeys_enabled` | 是否启用全局热键（顶栏「启用热键」是同一个开关） |
| `hotkeys` | 热键绑定，格式 `"槽位": "Ctrl+Alt+1"`，支持 `Ctrl/Alt/Shift/Win` 组合。`smaller` / `bigger` 是「一键」页用的「变小了 / 变大了」，默认 F8 / F9 |
| `presets` | 预设列表（工具自动维护） |
| `autobackup_saves` | 附加游戏时是否自动备份存档 |
| `auto_attach` | 游戏启动后是否**自动连接**（默认开）。连之前会等游戏加载完，避免扫不到东西 |
| `theme` | `dark` 或 `light`（也可点顶栏「切换主题」，重启后生效） |
| `ui_scale` | 界面缩放。`"auto"` 跟随 Windows 显示缩放（默认），也可写 `"150%"`、`"175%"` 或直接写倍数 `2.0`。与顶栏「界面缩放」是同一个设置 |
| `window_geometry` | 上次关闭时的窗口位置，形如 `"1800x1230+120+60"`。空串表示按当前缩放自动给一个合适尺寸；点顶栏「重置窗口」即可清空 |
| `scan.include_readonly` / `include_mapped` | 扫描范围是否包含只读 / 映射内存 |

---

## 十二、已知限制

1. **游戏更新后可能失效** —— 版本一变，地址与类布局都会变，需要重新定位。跑一次 `live_check.py` 就能知道工具本身是否还兼容。
2. **直接地址不抗重启** —— 游戏一关，地址就变了。用「指针链」形式（`模块名+偏移…`）才能跨重启复用；也可以每次重新定位后点「重新定位」覆盖预设。
3. **写内存有极小概率导致游戏崩溃** —— 这是所有内存修改器的通病。本工具的对策是：附加时自动备份存档 + 还原前的二次安全备份。建议**重要进度先手动备份一次**。
4. **F1~F5（以及 F8 / F9）会抢占游戏按键**（见第六节）。
5. 本工具只改了「数值」，不涉及游戏档案格式，**不会**影响成就/存档结构。
6. **「一键」页找数值需要你配合一次**：工具没法预先知道地址（每次开游戏都会变），
   所以必须"填一次数字 + 让数值变化后点一下按钮"。这是所有内存修改器的固有代价，
   不是工具偷懒 —— 但只需做一次，之后可以存成预设一键套用。
7. **把窗口拖到另一块缩放比不同的显示器后，Tk 不会自动重排**（Tk 8.6 的限制，
   它不处理 `WM_DPICHANGED`）。工具会在日志里提示当前 DPI 与启动时不一致，
   重启一次即可按新屏幕排版。
8. **改「界面缩放」需要重启修改器**才生效（同上原因）。选完会自动问你是否重启并代为重启；
   重启后锁定项不会自动恢复，需要重新套用预设。

---

## 十三、更新日志

### v1.2

**适配游戏 v0.30f**

- **游戏 v0.30 的 `_debug.txt` 新增了两个开关**，工具已认识并补上中文说明：
  - `streaming` —— 直播模式（作者直播时使用，会隐藏部分画面内容）
  - `nocomputeshader` —— 禁用 Compute Shader（画面异常或显卡驱动不兼容时可试）

  在 v0.29a 上打开时不会出现这两行，**同一个工具两版通吃**
- **自动识别游戏目录的能力增强**：v0.30 官方压缩包解开后是**两层嵌套**目录
  （`…Win ver030f\AliceInCradle Win ver030\AliceInCradle_ver030\`）。
  以前把 `AIC修改器` 放在最外层会认不出游戏（只向上找 3 层，全是空壳目录）；
  现在认不出时会**自动向下浅搜**，放在任意一层都能认出来
- **实机验证（针对 v0.30f）**：附加成功、检测到 Mono 运行时 `mono-2.0-bdwgc.dll`、
  主程序基址 `0x140000000`、全进程搜 4 字节耗时 **0.31 秒**、即时复核一致率 **100%**
- 验证：**10 个自测套件 + 界面冒烟 + 实机体检全部通过**
- 顺带一提：官方自 v0.29 起**禁止在虚拟机里运行游戏**（防盗版策略），
  且 Windows Defender 有时会把 `AliceInCradle.exe` 误报成威胁 —— 这两件都与本工具无关

### v1.1

**新增「一键」页：不用懂扫描原理也能用**（本轮改动的主线）

- 打开就停在 **「一键」页**，四步照做即可：① 连游戏 ② 点开关 ③ 找金币/物品 ④ 一键还原
- **第 2 步把游戏自带的调试开关做成大按钮**（免伤 / 攻击超强 / 全技能 / 相册 / 解锁 F7 菜单），
  按钮上直接显示 `● 已开启 / ○ 未开启`，一点即切
- **第 3 步把「扫描 → 再筛选 → 减少了」整套流程包成两步**：
  填一次数字 → 「开始找」→ 去游戏里让数值变化 → 点「变小了 / 变大了」→ 收敛后一键锁定/存预设。
  用户按的按钮上写的就是他刚做过的事，所以不可能选错筛选模式
- **新增 F8 / F9 全局热键**：在游戏里直接按，等于点了那两个按钮，不用来回切窗口
- **同一数值的多份拷贝会自动一起锁定**：游戏内部常把同一个数值存两份（显示用 + 逻辑用），
  它们永远同值同变，新手分辨不了 —— 工具识别出这种情况后一并锁上，并明确告诉用户
- **改数值时不再需要理解"字节宽度"**：四种宽度同时保留、让「变化」来淘汰。
  以前只挑"首扫命中最少"的那种宽度，可能挑中一个**根本不含真地址**的宽度（实测就会出现），
  用户白忙一场；现在宽度猜错也会被自动淘汰
- **首次查找从 6~10 秒降到 2~3 秒**：只扫主力宽度（4 字节 / 2 字节），
  1 字节 / 8 字节降级为兜底（实测主力宽度已能覆盖 64 位存储的小数值）。界面同时显示扫描百分比
- **自动连接**：游戏启动后自动连上，不用记得点按钮；并且**会等游戏加载完再连** ——
  实测刚启动的游戏进程"模块 0 个、可写内存 0 MB"，那时附加进去扫描会一无所获
- 一键页与调试页改为**可滚动区域**，窗口再小也点得到下面的按钮
- 修掉滚动区的一个缺陷：鼠标停在卡片/下拉框上时滚轮会失效（绑定方式从 Enter/Leave 改为全局分发）
- 新增 `core/autofind.py`、`ui/tab_onekey.py`、`tests/selftest_onekey.py`
- 自测脚本改为把配置写进**临时目录**（`AIC_CONFIG_DIR`），不再污染你真实的 `config/trainer.json`
- 验证：**10 个自测套件全过**；真实游戏实测 —— 301 MB 可写内存、首次查找 2~3 秒、
  单轮收敛 0.16 秒；「一键」流程用一个可远程改值的子进程端到端验证（填数 → 收敛 → 锁定 → 子进程的值真的变成 999999）

### v1.0.2

**界面按真实分辨率渲染（高 DPI 支持）+ 三处「内容被挤掉」的修复**

- **界面发虚的根因**：程序没有声明 DPI 感知，Windows 在 150% 缩放下只让它按 96 DPI
  绘制（2560×1440 的屏幕实际只用了 1707×960），再整体位图放大 1.5 倍。
  现在启动时声明**每显示器 DPI 感知 v2**，界面按真实像素绘制。
- **界面等比缩放**：字号走 Tk 的 `tk scaling`，padding / 表格行高 / 列宽 / 窗口尺寸
  走统一的 `px()` 换算，倍数严格一致，不会出现「字变大但间距没变」的挤版。
- **顶栏新增「界面缩放」**（自动 / 100%~200%）与 **「重置窗口」**；窗口大小与位置会被记住。
  缩放取下拉框里的**实际倍数**，「自动」= 跟随 Windows（你这里是 150%）。
- **修复：底部日志区被主区域挤成 1 像素**（等于看不见）。原因是 Tk 的 pack 先摆的先占位置，
  而标签页的请求高度很大；改成先把日志垫在底部再摆主区域。
- **修复：顶栏按钮被过长的游戏路径挤出窗口右边界**（`启动并附…` 被截断、右侧按钮点不到）。
  改为两行布局 + 长路径掐头显示；自测里加了「按钮是否都在可视区内」的断言。
- **修复：「调试开关」页二十来个开关有近一半看不到**（需要 1407px，实际只有 850px）。
  该页改为可滚动区域，支持滚轮。
- **新增** `tests\selftest_scaling.py`：验证 DPI 声明、缩放换算、窗口几何夹紧；
  `smoketest_ui.py` 增加按钮可见性与日志区可见性断言。
- 验证：8 个自测套件全过；实测 150%（自动）与 200% 两档缩放下，五个页面均无控件被裁。

### v1.0.1

**修复：首扫偶发漏掉整个内存区域**（这是上一版留下的隐患，已定位并修掉）

- **现象**：点「开始扫描」后偶尔少算几个命中，甚至整块目标区域的值一个都没扫到 —— 明明就在那儿。
- **根因**：`regions()` 给出的内存区域快照，是枚举那一瞬间拍的；而地址空间随时在变
  （堆在扩、页在释放）。旧实现一次 `ReadProcessMemory` 最多读 8MB，
  **只要这 8MB 里夹进一页读不到的画面，整块数据就被判定失败、整块丢弃**。
  单进程自测里这个概率约 30%，游戏里表现为「99.4% 一致率」「命中数忽多忽少」。
- **修法**：整块读失败时二分下探到 4KB 页粒度，**只丢真正读不到的那几页**；
  连续可读的部分自动合并回一片，正常情况仍是一次读取，速度不变（实测 0.42s，还略快了）。
- **顺带修掉**：分块扫描的跨块重叠区（防骑缝漏值多读的 3 字节）会把同一条命中
  记录两次，导致命中数虚高、重复地址混进结果表。现在按「本块拥有的字节数」截断，结果无重复。
- **新增回归测试**：`tests\selftest_scan_robust.py` —— 用 `VirtualAlloc` 拿页对齐内存，
  人为把中间一页变成读不到，验证两侧数据仍能扫到、只丢那一页、命中无重复。
- 验证：修复前后同一复现场景 30 次 —— 漏命中由 **9 次降为 0 次**；
  原始扫描自测连跑 12 次全过；7 个自测套件全过；实机自检一致率 99.4% → **99.6%**。

### v1.0

- 首个版本：外部内存修改器 + 五个页面 + 全局热键 + `_debug.txt` 纳管 + 存档备份。
